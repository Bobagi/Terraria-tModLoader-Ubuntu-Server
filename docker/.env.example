# Copy this file to ".env" (same folder) and set your server password.
#   cp .env.example .env
#   chmod 600 .env          # keep the password readable only by you
#
# The .env file is git-ignored on purpose — never commit your real password.
TMOD_PASS=change-me-to-a-strong-password
